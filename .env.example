# ---- Web app (web/.env) ----

# Database connection.
# Dev default is SQLite. For Postgres in production, change the datasource
# provider in web/prisma/schema.prisma to "postgresql" and set a Postgres URL,
# e.g. DATABASE_URL="postgresql://user:pass@host:5432/guideflow?schema=public"
DATABASE_URL="file:./dev.db"

# Base URL the app is served from (used for share links & extension handoff)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Secret used to sign session cookies. Set a long random value in production.
AUTH_SECRET="change-me-to-a-long-random-string"

# Secret used to encrypt per-tenant TTS API keys at rest (AES-256-GCM).
# Optional — falls back to AUTH_SECRET. Rotating it invalidates stored keys
# (users would re-enter theirs). Set a long random value in production.
ENCRYPTION_KEY=""

# ---- AI: captions ----
# "browser" = heuristic captions generated client-side (no key, default)
# "claude"  = richer captions via the Anthropic API (server-side)
AI_PROVIDER="browser"
ANTHROPIC_API_KEY=""

# ---- AI: text-to-speech (voiceover) ----
# Per-tenant keys (set in Settings) take precedence over everything below.
# These are the OPERATOR FALLBACK when a user hasn't added their own key:
# "browser"    = Web Speech API in the client (no key, default)
# "espeak"     = offline server-side TTS via the espeak-ng binary (no key)
# "openai"     = OpenAI audio/speech (neural)      [needs TTS_API_KEY]
# "elevenlabs" = ElevenLabs TTS (neural)           [needs TTS_API_KEY]
# "google"     = Google Cloud TTS (neural)         [needs TTS_API_KEY = Google API key]
TTS_PROVIDER="browser"
TTS_API_KEY=""
# Optional overrides for the neural providers:
# TTS_MODEL="gpt-4o-mini-tts"
# TTS_VOICE="alloy"

# ---- Media storage ----
# "local" = writable .media/ dir served via /api/media (default; use a volume)
# "s3"    = S3-compatible object storage (AWS S3, Cloudflare R2, MinIO)
#           Requires @aws-sdk/client-s3 (shipped as an optional dependency).
STORAGE_DRIVER="local"
# S3_BUCKET=""
# S3_REGION="us-east-1"
# S3_ENDPOINT=""              # set for R2/MinIO; leave blank for AWS S3
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
